[project]
name = "tribe-backend"
version = "0.0.1"
description = "TRIBE v2 backend: parcellation + mesh export pipeline"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "nibabel>=5.2",
    "nilearn>=0.10",
    "matplotlib>=3.8",
    "trimesh>=4.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "python-multipart>=0.0.9",
    "opencv-python-headless>=4.8",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cases>=3.8",
    "pytest-cov>=5.0",
    "hypothesis>=6.100",
    "import-linter>=2.0",
    "httpx>=0.27",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["tribe_backend*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "unit: fast unit tests (default)",
    "integration: tests that load real atlases or fixtures from disk",
    "slow: GLB export, full animation bundle",
    "gpu: requires CUDA + tribev2 weights",
]

[tool.importlinter]
root_packages = ["tribe_backend"]

[[tool.importlinter.contracts]]
name = "Compartment isolation"
type = "forbidden"
source_modules = [
    "tribe_backend.parcellation",
    "tribe_backend.mesh",
    "tribe_backend.inference",
]
forbidden_modules = [
    "tribe_backend.control",
]

[[tool.importlinter.contracts]]
name = "Parcellation does not import mesh or inference"
type = "forbidden"
source_modules = ["tribe_backend.parcellation"]
forbidden_modules = ["tribe_backend.mesh", "tribe_backend.inference"]

[[tool.importlinter.contracts]]
name = "Mesh does not import parcellation or inference"
type = "forbidden"
source_modules = ["tribe_backend.mesh"]
forbidden_modules = ["tribe_backend.parcellation", "tribe_backend.inference"]

[[tool.importlinter.contracts]]
name = "API talks only to control + contracts"
type = "forbidden"
source_modules = ["tribe_backend.api"]
forbidden_modules = [
    "tribe_backend.parcellation",
    "tribe_backend.mesh",
    "tribe_backend.inference",
]
# control/ is the composition root and legitimately imports all three
# compartments. The api/ rule is about *direct* imports — it must reach
# inference/parcellation/mesh only through control. Ignore the indirect
# paths that go through tribe_backend.control.
ignore_imports = [
    "tribe_backend.control.factory -> tribe_backend.inference",
    "tribe_backend.control.factory -> tribe_backend.inference.protocol",
    "tribe_backend.control.factory -> tribe_backend.mesh",
    "tribe_backend.control.factory -> tribe_backend.parcellation",
]
